============
Installation
============

At the command line::

    $ easy_install django-attrs

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-attrs
    $ pip install django-attrs
